language: go

go:
- "1.13"

os:
- linux

services:
    - docker

jobs:
    include:
        - stage: setup
          name: 'Setup'
        - stage: lint
          name: 'Run QA'
        - script: ./scripts/app/run lint ./...
        - script: ./scripts/app/run format ./...
        - script: ./scripts/app/run test ./...
